language: ruby
rvm:
- 2.3.3
cache: bundler
addons:
  - postgresql: "9.6"
install:
  - gem update --system
  - bundle install --jobs=3 --retry=3
script:
  - bundle exec rake db:setup
  - bundle exec rake spec
cache: bundler
